[pytest]
asyncio_mode = auto
testpaths =
    flud/test
markers =
    integration: end-to-end or multi-component integration tests
    slow: tests that take noticeably longer than routine unit tests
    stress: high-concurrency or load-oriented integration tests
